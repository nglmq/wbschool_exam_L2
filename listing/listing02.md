Что выведет программа? Объяснить вывод программы. Объяснить как работают defer'ы и их порядок вызовов.

```go
package main

import (
	"fmt"
)


func test() (x int) {
	defer func() {
		x++
	}()
	x = 1
	return
}


func anotherTest() int {
	var x int
	defer func() {
		x++
	}()
	x = 1
	return x
}


func main() {
	fmt.Println(test())
	fmt.Println(anotherTest())
}
```

Ответ:
```
2
1

В первой функции возвращается именованное значение, поэтому перед возвратом значения defer увеличит x на 1.
Во второй функции defer выполняется после return.
```